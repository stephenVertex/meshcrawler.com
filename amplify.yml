version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p build
        - cp index.html build/
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths: []
