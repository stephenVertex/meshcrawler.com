version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p build
        - cp index.html people.html software-factory.html styles.css build/
        - cp -R assets content build/
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths: []
