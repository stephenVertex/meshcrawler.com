version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p build
        - cp index.html people.html styles.css build/
        - cp -R branding assets build/
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths: []
